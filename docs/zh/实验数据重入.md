# 实验数据重入

真实数据重入顺序为：原始 VNA/示波器数据导入 -> provenance gate -> quality gate ->
校准与 reference-plane 检查 -> Stage-I 比较 -> discrepancy ledger 更新 -> validation status
更新。必须同时提供实际 Tx/Rx 几何、线缆/连接器、距离/方向、仪器、校准和不确定度元数据。
真实文件替换合成文件时不修改解析器、比较 API 或 Stage-H 合同。


# 软件引用与学术署名说明

`CITATION.cff` 用于告诉使用者如何引用软件版本，不等同于论文作者资格，也不能替代论文、
数据集和第三方后端各自的引用要求。

- **项目软件**：引用正式发布的软件名、作者、版本、仓库或归档标识和发布日期。
- **项目论文**：仅在用户确定引用策略和具体论文后加入，不虚构 DOI。
- **第三方后端**：Afivo、PETSc、Open MPI、openEMS/CSXCAD 等按各自要求单独引用。
- **数据集**：真实实验数据和大型仿真数据使用自己的作者、版本、许可证和归档标识。

当前软件引用信息为：软件名 `Microgap Breakdown RF Simulation & Validation Toolkit`，
作者 `Zach`，版本 `0.1.0rc1`，仓库
`https://github.com/wonderfulczc/microgap-breakdown-model`，许可证 `Apache-2.0`。
`Zach` 为用户明确指定的软件作者署名形式，未自动扩展真实姓名；CFF 使用合法且保守的
`family-names: "Zach"` 表达。机构按用户原文记录为
`College of Integrated Circuits of Southeast University`，未提供 ORCID。

`authors` 应列实际承担本项目软件创作责任的软件作者，并按用户确认顺序填写。科学贡献者、
导师或顾问可在项目文档、贡献说明或 CFF 的适当角色中记录，但不应自动转化为软件作者。
第三方后端作者不是本项目作者，应通过第三方 notices 和后端 citation 处理。

CFF schema 的最低字段为 `cff-version`、`message`、`title`、`authors`。本项目正式发行
当前没有正式公开 release，因而省略可选的 `date-released`，不虚构日期。当前软件本身是
可引用对象；未来有正式相关论文后，可将论文加入 `preferred-citation`，但软件 citation
继续保留。第三方后端仍应按其各自要求单独引用。

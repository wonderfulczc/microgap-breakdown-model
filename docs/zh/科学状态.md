# 科学状态

工具通过不等于科学验证通过。当前保持：Stage J 工具包装通过；Stage I 科学验证为
`PENDING_REAL_EXPERIMENT`；公开科学验证为 `false`；350 MHz 本征 RF 为
`NOT_RESOLVED`；合成夹具只能产生 `SYNTHETIC_DRY_RUN`。H3 全波加载反馈、H4 绝对幅值
网格敏感性及 Stage5 全 Maxwell 参考仍是显式债务。

CLI 配置不能把上述状态升级为 `MEASURED`、`VALIDATED` 或 `VALIDATED_WITH_VNA`。


# 科研模型变更流程

所有科学变更使用 `docs/decisions/DEC-XXXX-主题.md` 和 `literature/change_requests/CR-XXXX.yaml`。
请求必须说明受影响模块/Stage、科学理由、现状与建议、测试、重跑范围和向后兼容性。

未来模型更新保留旧配置和结果，运行清单记录 `model_version`、`contract_version`、
`schema_version`。仅在确有科学需要时并存模型版本；RP-1 不人为创建新物理版本。


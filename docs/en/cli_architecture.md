# CLI architecture

The `microgap-rf` command is a thin, configuration-driven adapter over frozen
project modules. It provides `doctor`, `run`, `validate`, `report`, and
registry-only `literature impact` commands. The complete and authoritative
description is [Chinese](../zh/CLI架构说明.md).

